# Create a new scatterplot here with BMI vs. Insulin
# Scatterplot of Insulin vs BMI

import matplotlib.pyplot as plt

# Assign variables
x = dm['Insulin']
y = dm['BMI']

# Build scatterplot
plt.scatter(x, y, c=colors, cmap='inferno', alpha=0.6)

# Add a color bar
plt.colorbar()

# Add labels
plt.xlabel('Insulin')
plt.ylabel('BMI')
plt.title('Insulin vs. BMI')

# Show the plot
plt.show()
